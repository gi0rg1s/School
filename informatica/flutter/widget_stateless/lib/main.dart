import 'package:flutter/material.dart';

/// Codice minimale per creare un'app con widget senza stato
void main() {
  runApp(
    const Center(child: Text("W Flutter", textDirection: TextDirection.ltr)),
  );
}
